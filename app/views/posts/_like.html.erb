<%= form_with(model: current_user.likes.build, url: post_likes_path(post), data: { turbo: true }, class: "like-form") do |f| %>
  <div><%= hidden_field_tag :post_id, post.id %></div>
  <button type="submit" class="bg-transparent border-0 p-0 m-0 cursor-pointer text-gray-500">
    <i class="far fa-heart"></i>
  </button>
<% end %>

<%= form_with(model: current_user.active_follows.find_by(followee: @user), html: { method: :delete }) do |f| %>
   <%= f.submit t("profile.unfollow.title"), class: "bg-gray-200 text-black px-4 py-1 rounded mr-2 cursor-pointer" %>
<% end %>

<h2 class="text-2xl font-bold text-center mb-8 pt-10"><%= t("user.listUser") %></h2>
<div class="container grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mx-auto pt-10">
  <%= render partial: "user", collection: @users, as: :user %>
</div>

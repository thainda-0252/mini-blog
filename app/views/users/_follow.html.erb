<%= form_with(model: current_user.active_follows.build) do |f| %>
  <div><%= hidden_field_tag :followee_id, @user.id %></div>
  <%= f.submit t("profile.follow.title"), class: "bg-blue-500 text-white px-4 py-1 rounded mr-2 cursor-pointer" %>
<% end %>

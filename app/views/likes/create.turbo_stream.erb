<%= turbo_stream.update "like-button-#{@post.id}" do %>
  <%= render partial: "posts/unlike", locals: { post: @post } %>
<% end %>

<%= turbo_stream.update "like-count-#{@post.id}" do %>
  <%= @post.likes.count %>
<% end %>
